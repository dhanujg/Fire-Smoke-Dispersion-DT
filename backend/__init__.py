"""
Sub‑packages:
    utils       – path + config helpers
    ingest      – data acquisition
    simulators  – VSmoke & future 3‑D
    api         – FastAPI apps
"""
import logging

logging.getLogger(__name__).addHandler(logging.NullHandler())
